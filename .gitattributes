# docs/index.html is regenerated by dashboard.py running on Linux in CI;
# pinning LF avoids Windows checkouts showing it as modified due to
# autocrlf round-tripping.
docs/index.html  text eol=lf
dashboard.py     text eol=lf
